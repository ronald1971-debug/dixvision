# runtime.unified_fabric — Unified Event Fabric (Stage 5).
#
# Entry point: get_unified_event_fabric()  from runtime.unified_fabric.unified
# Authority:   get_central_bus_authority() from runtime.unified_fabric.authority
# Tracing:     get_event_tracer()          from runtime.unified_fabric.tracing
# Lineage:     get_event_lineage_graph()   from runtime.unified_fabric.lineage
# Persistence: get_fabric_persistence()   from runtime.unified_fabric.persistence
# Replay:      get_fabric_replay_stream() from runtime.unified_fabric.replay
# Bridges:     get_cognitive_bus_bridge(), get_execution_fabric_bridge()
