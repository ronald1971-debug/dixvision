"""runtime.governance — Live Governance Enforcement (CONVERGENCE PILLAR 3).

Governance becomes:
- Runtime-enforced (not advisory)
- Blocking (synchronous gate in execution path)
- Authoritative (HMAC-signed decisions)
- Deterministic (same inputs → same decision)

No intent passes without governance's cryptographic signature.
Fail-closed: missing signature = blocked.
"""
